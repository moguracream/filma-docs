# 無効化済み
#
# 以前は master の内容（template-* ディレクトリ等）を gh-pages ルートへ同期していた。
# 現在は .github/workflows/pages.yml で MkDocs のHTML生成と gh-pages 更新を行うため、
# master 全体を gh-pages へ同期しない。

name: Deploy existing HTML (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This workflow is disabled. Use .github/workflows/pages.yml for GitHub Pages deployment."
